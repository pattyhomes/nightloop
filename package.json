{
  "name": "nightloop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "build": "npm run --prefix backend build && npm run --prefix frontend build",
    "build:backend": "npm run --prefix backend build",
    "build:frontend": "npm run --prefix frontend build",
    "test:signal-ingestion": "npm --prefix backend exec -- tsx scripts/testSignalIngestion.ts",
    "test:event-signal-import": "npm --prefix backend exec -- tsx scripts/testEventSignalImport.ts"
  }
}
